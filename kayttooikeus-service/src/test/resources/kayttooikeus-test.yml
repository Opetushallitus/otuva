spring:
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.HSQLDialect
      implicit-naming-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyJpaCompliantImpl
      ddl-auto: update
    connection:
      charset: utf-8
    archive:
      autodetection: class,hbm
    show-sql: true
    format-sql: true
    current-session-context-class: org.springframework.orm.hibernate5.SpringSessionContext
    database-platform: org.hibernate.dialect.HSQLDialect
hikari:
  datasource:
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
    jdbc-url:  jdbc:hsqldb:file:target/auth
    username: SA
    maximum-pool-size: 5
    connection-timeout: 10000
    max-lifetime: 60000

# TODO: proper mock for these in tests
cas:
  ldap: {}
  service: https://localhost:8183/kayttooikeus-service
  send-renew: false
  key: kayttooikeus-service
  url: http://localhost:9292/cas
  fallback-user-details-provider-url: http://localhost:9292/userDetails/
